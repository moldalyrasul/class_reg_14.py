class Coustemuser:
    def __init__(self, first_name, last_name, email, password, age, gender, phone, secret_key):
        if isinstance(first_name, str):
            self.first_name = first_name
        else:
            raise Exception(' first name is string ')
        if isinstance(last_name, str):
            self.last_name = last_name
        else:
            raise Exception(' last name is string')
        if isinstance(email, str):
            self.email = email
        else:
            raise Exception(' email is strin ')
        if isinstance( password,str):
            self.password = password
        else:
            raise Exception('password is strinh')
        if isinstance(age, int):
            self.age = age
        else:
            raise Exception('age is integer ')
        if isinstance(
            
        )









